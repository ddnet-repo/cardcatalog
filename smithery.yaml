startCommand:
  type: http
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        description: "Card Catalog API key (starts with cc_). Register free at https://cardcatalog.ai/api/agents/register"
    required: []
  url: https://cardcatalog.ai/mcp
