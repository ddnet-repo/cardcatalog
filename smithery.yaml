startCommand:
  type: http
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        title: API Key
        description: "Card Catalog API key (starts with cc_). Optional — registration and discovery work without auth. Get one free at https://cardcatalog.ai/api/agents/register"
    required: []
  url: https://cardcatalog.ai/mcp
